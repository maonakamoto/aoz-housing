# PostgreSQL connection (self-hosted, like evig — no Supabase)
DATABASE_URL=postgresql://fitfoot:fitfoot@localhost:5432/fitfoot

# Session signing secret (generate with: openssl rand -hex 32)
SESSION_SECRET=change-me-to-a-long-random-string

# Absolute base URL for links in emails etc.
NEXT_PUBLIC_APP_URL=http://localhost:3005
